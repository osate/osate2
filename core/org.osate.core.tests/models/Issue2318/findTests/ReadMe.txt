These files are used to test that the findXXXInstnace() methods in ConnectionInstanceImpl 
  1) properly deal with refinement
  2) are not fooled by items with the same name but that aren't really in the component

 