===== annex[0] owner=ForDequeues::worker.i =====

        variables
            a, i : Base_Types::Integer;
        states
            start : initial state;
            done : complete final state;
        transitions
            start -[]-> done {
                for (i : Base_Types::Integer in 0 .. 2) {
                    feed?(i);
                    forall (j : Base_Types::Integer in 0 .. 2) {
                        feed?(j);
                        if (true) { do { feed?(i) } until (true) } end if;
                        feed?(a)
                    };
                    feed?(i)
                };
                feed?(i)
            };
\s\s\s\s
----- Xtext reparse -----
reparsed
