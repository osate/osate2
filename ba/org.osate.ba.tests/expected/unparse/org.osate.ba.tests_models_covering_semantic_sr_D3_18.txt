===== annex[0] owner=example_sr_D3_18::th.good =====

  states
    s0 : initial state;
    comp : final state;
    next1 : complete state;
\s\s\s\s
  transitions
    s0 -[ ]-> next1 { x1 := p1 };
    next1 -[ on dispatch p2 ]-> comp { x2 := p2 };
----- Xtext reparse -----
reparsed
===== annex[1] owner=example_sr_D3_18::th.error =====

  states
    s0 : initial state;
    comp : final state;
    next1 : complete state;
\s\s\s\s
    -- Error : next2 must be an execution state
    next2 : complete state;
\s\s\s\s
  transitions
    -- If s0 is only an initial state so we can have an execute condition
    s0 -[ x1 = 0 ]-> next1 { x1 := p1 };
    next1 -[ on dispatch p1 ]-> next2 { x2 := p2 };\s\s
    next2 -[ x1 = 2 ]-> comp { x2 := p2 };
----- Xtext reparse -----
reparsed
